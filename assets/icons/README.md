# Threshold icons

Copy the existing production icon files into this directory:

- `icon-192.png`
- `icon-512.png`
- `apple-touch-icon.png`

The refactored HTML, manifest and service worker already reference these paths.
